package = "spruce"
kind = "Major Release"
body = "spruce/group is merged into spruce/output. group.render_title is renamed to output.title, and group.group, which prints its heading and runs its body immediately, is renamed to output.stream_group. The buffered output.group is unchanged."
