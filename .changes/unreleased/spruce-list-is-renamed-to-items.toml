package = "spruce"
kind = "Major Release"
body = "spruce/list is renamed to spruce/items, and its List type is renamed to Items, so the module no longer conflicts with gleam/list or the built-in List type. The functions are unchanged, for example items.new() |> items.item(...) |> items.render(sp, _)."
