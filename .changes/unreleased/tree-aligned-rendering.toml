package = "spruce"
kind = "Added"
body = "spruce/tree adds tree.columns and tree.render_table for aligned tree rows, plus tree.render_with for label rendering based on prefix width, depth, and sibling position."
